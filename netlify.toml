[build]
  command = "npm install && chmod +x ./node_modules/.bin/* && npx vite build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200